import { utilCOMPONENT_NAME } from "./COMPONENT_NAME.utils";
/*
  Unit tests for the COMPONENT_NAME component.
*/

const obj = { foo: "bar" };

test("example test for utilCOMPONENT_NAME", () => {
  expect(utilCOMPONENT_NAME(obj)).toBe(obj);
});
